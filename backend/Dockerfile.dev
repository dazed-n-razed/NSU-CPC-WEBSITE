# Development Dockerfile for Backend
# Use this for hot reload with nodemon

FROM node:18-alpine

WORKDIR /app

# Install dependencies
COPY package.json package-lock.json ./
RUN npm install

# Copy source code
COPY . .

# Expose port
EXPOSE 5000

# Run with nodemon for hot reload
CMD ["npm", "start"]
